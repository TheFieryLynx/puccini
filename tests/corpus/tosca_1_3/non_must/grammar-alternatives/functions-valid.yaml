tosca_definitions_version: tosca_simple_yaml_1_3
topology_template:
  inputs:
    left:
      type: string
      default: left
  outputs:
    joined:
      type: string
      value: { concat: [{ get_input: left }, "-right"] }
