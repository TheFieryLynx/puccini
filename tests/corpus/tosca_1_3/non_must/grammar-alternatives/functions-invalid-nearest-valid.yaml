tosca_definitions_version: tosca_simple_yaml_1_3
topology_template:
  inputs:
    source: {type: string, default: value}
  outputs:
    result:
      type: string
      value:
        get_input: source
