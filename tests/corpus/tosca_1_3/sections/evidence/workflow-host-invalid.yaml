tosca_definitions_version: tosca_simple_yaml_1_3
node_types:
  Holder:
    derived_from: tosca.nodes.Root
topology_template:
  node_templates:
    server: {type: tosca.nodes.Root}
    app:
      type: Holder
      requirements:
        - dependency: server
  workflows:
    main:
      steps:
        run:
          target: app
          target_relationship: dependency
          # omitted
          activities: []
