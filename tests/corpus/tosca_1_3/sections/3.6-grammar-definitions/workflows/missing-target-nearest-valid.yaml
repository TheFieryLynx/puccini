tosca_definitions_version: tosca_simple_yaml_1_3
topology_template:
  node_templates:
    node: {type: tosca.nodes.Root}
  workflows:
    main:
      steps:
        run:
          target: node
          activities: []
