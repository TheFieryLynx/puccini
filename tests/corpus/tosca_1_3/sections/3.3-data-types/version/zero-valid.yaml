tosca_definitions_version: tosca_simple_yaml_1_3
node_types:
  VersionHolder:
    derived_from: tosca.nodes.Root
    properties:
      release:
        type: version
topology_template:
  node_templates:
    node:
      type: VersionHolder
      properties:
        release: "0.0.0"
